# Settings for Backend (on Cloud Run).
# See https://firebase.google.com/docs/app-hosting/configure#cloud-run
runConfig:
  minInstances: 1  # 最低1インスタンスで待機時間を短縮
  maxInstances: 50  # より多くのリクエストに対応
  concurrency: 200  # 同時実行数を大幅に増加
  cpu: 4  # CPUパワーを4倍に増強
  memoryMiB: 4096  # メモリを4GBに大幅増量でパフォーマンス向上