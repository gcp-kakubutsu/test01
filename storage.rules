rules_version = '2';

service firebase.storage {
  match /b/{bucket}/o {
    // Allow read and write access for all paths (completely open for development)
    match /{allPaths=**} {
      allow read, write: if true;
    }
  }
}