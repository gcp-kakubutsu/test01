# タスク完了時のチェックリスト
- 変更内容に応じて `npm run lint` と `npm run typecheck` を実行し、静的解析と型チェックを通す。
- 該当するユニット/統合テストがあれば `npm run test` または対象スイートを実行。
- UI改修時はローカルで `npm run dev` を立ち上げ、画面挙動と重要なユーザーフローをブラウザで確認。
- Firebaseや外部APIの設定変更が必要な場合は、READMEや関連ドキュメントに従い環境変数や設定ファイルを更新したか確認。