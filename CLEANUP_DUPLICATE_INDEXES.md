# Firebase重複インデックスの削除手順

## 現在の状況
Firebaseコンソールに同じ`posts`コレクションのインデックスが2つ存在しています：
1. `communityId` + `timestamp` + `__name__` (CICAgQjXh4EK) - 有効
2. `communityId` + `timestamp` + `__name__` (CICAg.JiUpoMK) - 有効

## 削除手順

### 1. どちらか一方を削除
1. [Firebase Console](https://console.firebase.google.com/project/nukune/firestore/indexes)にアクセス
2. 2つの重複した`posts`インデックスのうち、**1つを残して、もう1つを削除**
3. 各インデックスの右側にある「⋮」メニューをクリック
4. 「削除」を選択
5. 確認ダイアログで削除を確定

### 2. どちらを削除すべきか
- 両方とも同じ設定なので、どちらを削除しても問題ありません
- 通常は**新しい方（作成日時が新しい方）を削除**することを推奨

## 削除後の確認
1. インデックスが1つだけになったことを確認
2. アプリケーションでコミュニティページにアクセス
3. エラーが出ないことを確認

## なぜ重複が発生したか
- エラーメッセージのリンクから複数回インデックスを作成した
- Firebase CLIとコンソールの両方から作成しようとした
- 同じインデックス設定を複数回実行した

## 今後の防止策
1. インデックスは一度だけ作成する
2. 作成前に既存のインデックスを確認する
3. `firestore.indexes.json`ファイルを使用してインデックスを管理する